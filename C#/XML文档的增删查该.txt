追加XML文档：
1.首先判断当前文件是否存在
获取指定节点下的节点的值 innertext
节点名称["需要获取的节点的名称"].innertext

  
if(File.Exists("XML文档路径"))
2.如果存在的话则先加载XML文档
doc.Load("XML文档")
3.获得这个XML文档的根节点
doc.Documentelement();
4.添加数据即可
5.如果不存在的话：
6.创建第一行，根节点，添加数据即可


查看XML文档
	1.不带属性的
		找到根节点，在使用childNodes寻找下面的所有子节点。
		遍历，输出innertext即可
	2.带属性的
		找到根节点，在使用根节点索引方式寻找到需要查看的子节点 Order["Items"]
		遍历，输出Attibute["属性"].value即可
		
修改属性的值
	1.判断当前的属性的值是否为你所需要修改的值
	if(item.att.....["属性"].value=="属性对应的值")
	2.修改即可
	itme.att.....["属性].value="需要修改为的值";
	
删除节点：
	节点.RemoveAll()：移除根节点下的所有子节点
	
使用 XPath 方法来获取和删除属性
	获取属性的值：
	   1.通过获取的根节点：preson.selectsingleNode("获取的根节点的绝对路径[@属性='值']")，返回一个
	     Xmlelement类型变量,比如说：xn
	   2.xn.att..["属性"].value
	删除带有属性的节点：
		如果要删除带有属性的节点，应该使用直系父节点来删除，也就是包含这个节点的子节点
		删除指定节点，带有属性的节点
			1.获取要删除的节点的直系父节点   1
			2.获取要删除的节点的属性的值 使用XPath    2
			3.1.RemoveChild(2);  移除当前节点
	删除指定节点的属性和属性的值：我们需要删除那个属性就必须拿到该节点的直系父节点
		1.首先获取该节点说在的位置 Order.selectsingleNode("获取的根节点的绝对路径[@属性='值']");
		2.xn.attributes.RemoveNamedItem("属性名字");
			
	
	
	